spring.application.name=backend
server.port=8080

# MongoDB
spring.data.mongodb.uri=${MONGODB_URI}

# Google OAuth2 Configuration
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.redirect-uri=http://localhost:8080/api/auth/google/callback
spring.security.oauth2.client.registration.google.scope=profile,email

# OAuth2 Client Provider
spring.security.oauth2.client.provider.google.authorization-uri=https://accounts.google.com/o/oauth2/auth
spring.security.oauth2.client.provider.google.token-uri=https://oauth2.googleapis.com/token
spring.security.oauth2.client.provider.google.user-info-uri=https://www.googleapis.com/oauth2/v3/userinfo

# JWT
app.jwt.secret=${JWT_SECRET:defaultSecretKeyForDevelopment123456789}
app.jwt.expiration=${JWT_EXPIRATION:86400000}

# Logging
logging.level.com.smartcampus.backend=DEBUG
logging.level.org.springframework.security=DEBUG


